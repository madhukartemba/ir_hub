#pragma once

// Copy this file to include/secrets.h. The build copies it automatically when
// include/secrets.h is missing. Never commit secrets.h.
//
// All four values below are *compile-time defaults*. They are used only when
// LittleFS has no `/mqtt.json` (i.e. the device has never been configured).
// After Wi-Fi/MQTT setup via the captive portal, the entered values are saved
// to the device and override these defaults.
//
// Ship the firmware with these left blank. An empty MQTT_HOST disables MQTT
// entirely until the user configures it from the captive portal.

#define MQTT_HOST ""
#define MQTT_PORT 1883
#define MQTT_USER ""
#define MQTT_PASSWORD ""

// ----- ArduinoOTA (LAN push) -----------------------------------------------
// Password required to push firmware over Wi-Fi from your laptop. Leave blank
// to allow unauthenticated push (NOT recommended for shipped devices).
#define OTA_PASSWORD ""

// ----- HTTP-pull OTA (Cloudflare Pages) ------------------------------------
// URL of the JSON manifest the device polls for new firmware.
// Leave blank to disable the pull-based updater entirely.
//
// Recommended host: Cloudflare Pages. It supports TLS MFLN (RFC 6066), which the
// ESP8266 needs to do HTTPS with the small heap budget it has.
// raw.githubusercontent.com (Fastly) and jsDelivr work from a laptop but crash the
// device's WiFi stack mid-handshake on low-heap boards.
//   #define OTA_MANIFEST_URL "https://<your-project>.pages.dev/ota/manifest.json"
#define OTA_MANIFEST_URL "https://ir-hub.pages.dev/ota/manifest.json"
